# Run for PR on master to block merge if any workflows fails.
name: CI Operator

on:
  push:
  workflow_dispatch:
  pull_request:
    branches:
      - master

jobs:
  lint:
    uses: ./.github/workflows/lint.yaml

  test:
    uses: ./.github/workflows/test.yaml
